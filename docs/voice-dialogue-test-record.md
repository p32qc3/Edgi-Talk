# 语音对话整合验证记录

日期：2026-09-10

## 已通过

- ECS 服务自动测试：40 项通过。
- 板端通信、AI 指令限制、语音状态、共享录音缓冲和界面字体检查：11 组通过。
- ECS 公网 `http://47.116.168.34/healthz` 返回正常，错误令牌会被拒绝（HTTP 401）。
- 板端 Wi-Fi、ECS 地址和设备令牌已写入两个本地私密配置文件；API Key 仅保留在 ECS。
- M33 从干净状态编译成功：`text=160400, data=14032, bss=244565`。
- M55 从干净状态编译成功：`text=1041728, data=4592, bss=4547708`。
- 已按 M33、M55 的顺序完成烧录，两个镜像均通过写入后校验。
- 真机启动检查通过：M33 正常运行并启动 M55，M55 未进入崩溃或自检失败处理。
- 修复了 M33 语音线程缓冲区挤占运行空间导致的启动崩溃。
- 修复了 M55 与 M33 争用调试串口导致的启动自检失败；M55 现在只注册与 51 通信所需的串口。
- 语音共享区为 `0x261C1000..0x261D0FFF`，位于链接文件预留区域内，不与 M55 堆或显示缓冲重叠。
- Git 追踪文件中未包含百炼 API Key、Wi-Fi 密码或设备令牌；真实配置文件已忽略。

## 当前已烧录固件

- M33：`D:\edgi_pet\output\Edgi_Pet_M33.hex`
  - SHA-256：`5F21A1666056B8BA376D64C3BFDB8FFD378FD63CB4E7B25351AA621648DFD18F`
- M55：`D:\edgi_pet\output\Edgi_Pet_M55.hex`
  - SHA-256：`9AA04AA49D68B65DC97D797C935100FF45080C4AD60A207AC07DA22E1333C053`

## 现场状态与剩余验收

- 语音服务已启动并进入 Wi-Fi 连接阶段。
- 板端配置的热点名称是 `edgi`，但现场扫描不到该热点，因此暂时无法完成板子到 ECS 的真实语音请求。
- 打开同名的 2.4 GHz 热点后，确认屏幕由“连接中”变为“可对话”，再点击麦克风完成普通中文对话、查看状态、记忆挑战、断网重连和超时恢复测试。
- 完整脱离电脑运行还需最后拔掉数据线，用普通供电线供电复测一次。

## 回退

本次烧录前的固件已保留在 `D:\edgi_pet\output\previous\before_voice_20260910_160815`。如需回退，仍按 M33、M55 的顺序烧回这一对固件。
